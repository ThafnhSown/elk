filebeat.inputs:
- type: docker
  containers.ids: 'd927348bd4ed204cf418c389550d8b0f88812646bc7cd44740186bf670295631'


filebeat.autodiscover:
 providers:
   - type: docker
     hints.enabled: true


processors:
- add_docker_metadata: ~


setup.kibana:
 host: ${KIBANA_HOSTS}
 username: ${ELASTIC_USER}
 password: ${ELASTIC_PASSWORD}


output.logstash:
  hosts: ${LOGSTASH_HOSTS}